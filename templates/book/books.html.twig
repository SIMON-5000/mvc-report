{% extends 'book/library-template.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block article %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95vw; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello booklovers</h1>

    <p>Vårt bibliotek börjar ta form, vi har än så länge dessa kvalitetstitlar att välja mellan:</p>
    <ul>
        {% for book in books %}
            <li class="book-preview">
                <img 
                    class="img-book-preview"
                    src="{{ asset('img/'~book.getImg ) }}"
                    alt="{{ book.getTitle|e }}" 
                    onerror="this.onerror=null;this.src='{{ asset('img/book.png') }}';"
                    height="50"
                >
                <a href="{{ path('book_by_id', {'id': book.getId|e } ) }}">{{ book.getIsbn|e }} <b>{{ book.getAuthor|e }}</b> {{ book.getTitle|e }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
